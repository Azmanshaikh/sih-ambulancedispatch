{
  "name": "jeevan",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "backend": "cross-env PYTHONPATH=./backend uvicorn app.main:app --reload --port 8000"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.4",
    "autoprefixer": "^10.5.0",
    "cross-env": "^10.1.0",
    "tailwindcss": "^4.2.4",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.104.1"
  }
}
